#include "symbolInfo.h"

#ifndef _SYMTAB_H
#define _SYMTAB_H

/*
 *	Symbol table consists of a basic cell containing symbol information and a hash table containing
 *  cells of symbol information.
 *
 *	To add a symbol, simply call add(symbolName, Type, Scope) to add a new symbol.
 *	To find a symbol, call find(symbolInfo symbol)
 * 	To remove a symbol, call remove(symbolInfo toRemove)
 */


#endif
